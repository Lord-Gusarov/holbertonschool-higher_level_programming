-- Task: List all records from second_table, showing just score and name.. ordered by score
-- Selecting the records, the rows desired and how to order them
SELECT score, name FROM second_table
ORDER BY score DESC;
